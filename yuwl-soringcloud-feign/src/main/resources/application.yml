server:
  port: 8860

spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8800/eureka/
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true
