server:
  port: 8870
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  application:
    name: nativeconfigserver